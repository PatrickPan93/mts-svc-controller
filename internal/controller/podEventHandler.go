package controller

/*
func (c *Controller) HandlePodAddEvent(namespace, name string) bool {
	pod, err := c.podLister.Pods(namespace).Get(name)
	if err != nil {
		klog.Errorf("Error while handling pod object: namespace(%s) name(%s) %s", namespace, name, err)
		return false
	}
	annotations := pod.GetAnnotations()
	configs, ok := annotations[CNCFV1NetworkKey]
	if !ok {

	}
	return true
}

*/
